# 当前任务｜ARCH-001 收口

更新：2026-09-14。将首版租户隔离选择和基础必读规则通过PR #7收口，不实施业务数据库。

## 目标

把用户接受的“按服务分库、服务内严格隔离公司、独库按需另审”写进AGENTS及相关文档，保留所有安全/验证边界，完成最终提交检查、独立审查和本次获准的main合并。

## 范围

沿用PR #7，更新AGENTS、既有handoff Skill、26基础决策、短状态、当前任务、同一ARCH-001学习笔记及生成索引。D-TENANCY-01从待定变为首版设计已接受；本轮显式main指令替代上轮未放行。

不改业务代码、脚本、测试、CI或权限，不安装/迁移数据库，不配置生产、付费模型或CMS。自动记忆刷新不等于常态自动合并。

## 验收

根AGENTS直接包含已确认的基础约束，仍≤12KiB；文档/Skill/状态无互相冲突的租户待定表述。服务所有权与租户隔离分开，RLS仅附加防线，连接复用/缺上下文拒绝/跨公司资源/撤权/恢复是实际业务验收门槛。角色矩阵与公司内共享策略仍未批准，不因用户同意存储方案授予全部成员所有权限。

完整head的治理检查、54项既有工具回归、索引一致性/差异防漏记及独立审查按实际结果记录；不得把这些称为多租户业务通过。合并后回读main、检查结果及Issue #4，不凭请求发送成功宣布合并。

## 未执行与阻塞

本轮容器git ls-remote仍因GitHub DNS失败退出128；连接器承担远端读写，本地只整理已核对源码字节的文档与索引，不声称完整clone或全套本地测试。完整仓库验证来自本PR实际CI。

PostgreSQL/RLS/连接池/Worker/私有文件缓存检索隔离、单公司导出/删除/恢复、模型/CMS/Redis业务及用户机器Skill加载均未执行。负责人为项目负责人；补测需分别约定隔离测试范围、接口/权限及恢复目标。没有新增本人代码实践反馈。

## 下一步

合并结果以PR #7最终交付记录及实时页为准；收口后讨论“公司内谁能看、谁能改、谁能发起耗费模型额度的任务”。下一轮拟定角色/资源/动作矩阵及成员邀请撤销边界，先提案再确认，不提前生成用户系统或默认全员可读全部资料。

DATA-001原公告/更正日期走查和CMS单流程仍保留。不要为了写入自身合并SHA制造递归提交，接棒时实际核对远端并更新语义状态。
