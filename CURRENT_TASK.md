# 当前任务｜DOC-003

更新：2026-09-12。工作边界：规范、项目交接和中文 CMS 门槛；不是整套业务开发。

## 目标

把通用研发原则改写成 BidRadar 可执行规则，汇入此前后台/CMS/缓存设计，减少长上下文依赖，并在满足独立审查与验证后按本次明确授权通过 PR 合并 main。

## 范围

更新 AGENTS/README/交接入口；新增研发规范、短状态、上下文索引、中文 CMS 验收、交接 Skill、PR 模板和标准库检查工具。保留现有产品架构、预算、真实证据与私有数据边界，不复制公司名或内部原文，不安装 CMS、不生成业务空骨架、不采购部署。

## 验收

规则明确 Codex 执行/人工决策；授权、接口、模拟/真实、自测、独立审查、运行隔离、费用和恢复可核查；AGENTS/状态保持短小，相关文件可定位；中文 CMS 要求有具体负例；实际命令和最终提交在 PR/CI 留证。不能把脚本静态检查通过等同于独立审查、业务验收或仓库保护已开启。

本轮验证入口：`python3 scripts/check_project_memory.py`、`python3 -m unittest discover -s tests -p 'test_*.py' -v` 和对应提交差异检查。记录具体环境、SHA、退出码及未执行项。

## 未执行与阻塞

本地 GitHub 克隆尝试遭 DNS 解析失败，改用授权 GitHub 连接器读取与提交；远端 CI 的运行与结果需实际回读。CMS/Redis/浏览器/第三方真实联调、云环境与产品性能均未执行；这些不属于本次已完成范围。

独立审查不能由开发会话自己冒充；缺少最终提交对应的审查或必要检查时保留 PR，不以用户的一般合并意愿跳过。仓库保护、独立审查和合并的最终状态以 PR/实际配置为准；本次不宣称已经建立无人值守发布。

## 下一步

DOC-003 收口后，只细化并授权一条“中文帮助文章草稿→发布→公开读取及缓存命中→修改失效→撤下拒绝访问”的薄验证任务。先确定接口、角色、测试环境和验收，不同时铺开五类后台，也不阻塞招标研究主线。若本轮合并受阻，先补齐审查/验证阻塞，再进入该任务。
