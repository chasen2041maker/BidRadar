# 当前任务｜ARCH-001

更新：2026-09-13。PRODUCT-001已由PR #6合并；本轮只补基础决策与代理必读规则。

## 目标

记录用户对平台型交付、数据边界、版本、持久任务、接口和受控试用的确认；解释公司独库的利弊，不把询问误记为批准；每次代理新任务或接棒实际读取这些基础约束。

## 范围

更新AGENTS必读章节、现有handoff Skill、短状态、当前任务；新增26基础决策/存储取舍及ARCH-001学习笔记，重建索引。不改业务代码、测试、CI/权限，不建库、不迁移、不安装CMS、不使用真实企业数据或付费模型。

本次有文档写入授权，未收到本任务合并指令。保持任务分支/PR；不以此前任务的一次性放行自动合并。

## 验收

RQ-28～33确认范围明确，D-TENANCY-01仍待决定；业务空间、服务数据所有权和数据库/实例分开；根AGENTS直接包含确认要点，不只依赖Skill匹配。Skill要求真实读取、基线及适用约束，不能假装配置宿主hook。

原中文注释/学习、独立审查、最终提交测试、人工放行、隐私和预算边界不降低。AGENTS≤12KiB；索引一致并按任务留笔记；必要检查/独立审查绑定实际head，结果放PR而不预填成功。

## 未执行与阻塞

本轮容器git ls-remote因GitHub DNS失败，退出128；连接器用于远端读写。本地只有已核对字节的文档副本和索引整理，不声称完整checkout或全套本地测试；完整仓库检查由本PR的实际CI提供。

未执行租户数据库/RLS/连接池/Worker/缓存隔离、单公司导出恢复、真实接口/模型、CMS/Redis/云部署、用户宿主的必读/隐式Skill触发。上述不是文档测试能证明的能力；租户方案未定前不建表。

## 下一步

只继续比较D-TENANCY-01：所属服务内共享库的隔离实验和按服务×租户独库的迁移/恢复成本；先明确公司数据隔离与恢复要求，再由用户定案。业务DATA-001仍作为小步走查保留。

学习入口为26第3～4节：用户尚未反馈亲自读代码或运行测试，不能自动标已掌握。合并及主分支规范生效需本任务放行和最终审查/检查。
