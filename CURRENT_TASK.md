# 当前任务｜DOC-004

更新：2026-09-12。DOC-003 已在 PR #3 收口；本轮只完善学习和记忆工作流，不实施 CMS 业务。

## 目标

让负责人能通过读关键代码和小型实践参与项目；将接棒、有效更新、提交/合并收尾变成主动执行的 Skill 和可验证的自动记忆流程。

## 范围

AGENTS 中文注释/学习要求；扩展现有 bidradar-handoff Skill；按任务保存有用决定、代码阅读、失败和参与状态；生成索引、检测漏记；CI 在推送/PR 生成摘要，main push 仅更新固定状态页 Issue #4，不直接写 main。

## 验收

新改关键逻辑中文解释完整，有 1–3 个源码符号阅读入口和一个小练习；真实学习状态不被合并推断。相同输入重复生成无差异；状态或笔记改变可发现旧索引；代码变更漏笔记、目录越界被拒绝；分支/旧提交不能覆盖 main 状态，关闭状态页可停止发布。保留独立审查、最终提交验证与既有合并授权边界。

命令：`python3 scripts/refresh_project_memory.py --write`、`--check`，以及 AGENTS 的完整工具测试与提交差异检查。代码/CI 实际运行结果放对应 PR，不在此提前宣称通过。

## 未执行与阻塞

本机 GitHub DNS 解析失败，远端读写通过 GitHub 连接器；本地仅运行隔离工具/模拟 API 测试，完整仓库以远端 CI 为准。当前 Skill 自动调用未在负责人电脑实测；模拟 API 通过不等于已向 Issue #4 真实发布，合并后需回读。

CMS/Redis/中文浏览器、真实招标数据/模型、云环境没有运行。缺独立审查或必要检查不合并；自动记录机制不赋予无人值守合并权限。

## 下一步

先让负责人读 [DOC-004 阅读导图](docs/learning/DOC-004.md)，理解“源笔记、生成索引、实时快照”三者。为其保留一个修改笔记后观察过期检查的小练习，待反馈再标学习进度；不抢先声称其已完成。产品侧下一小步仍是中文帮助文章发布/缓存/撤下流程的接口与隔离验证约定。
