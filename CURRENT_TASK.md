# 当前任务｜ACCESS-001：权限第一小片与带读

更新：2026-09-14。用户同意权限1～4项并要求保存main、开始小步实现。当前范围仅一个纯函数及相应测试，不是整套用户系统。

## 目标

落实三角色、共享/受限资料、动作权限与邀请撤权规范；写好中文关键注释，完成“只读成员可看普通报告但不可请求分析”的可运行领域规则，停在阅读检查点。

## 范围

1个核心实现文件services/workspace/domain/access_policy.py，1个测试文件tests/test_workspace_access_policy.py；同步AGENTS、已有Skill、27、状态、当前任务和学习索引。规则与首片在同一小任务内审查，不铺第二个业务模块。

本次经审查合并到main已获明确授权；不直接推main、不扩大CI权限，不安装数据库/登录/CMS/Redis、不抓取/部署/付费或处理真实企业资料。设计与纯函数通过不是全系统鉴权通过。

## 验收

正常：同公司活跃三角色可读普通报告，仅管理员/协作成员通过请求分析的基础动作检查。异常：缺失/移除成员、跨公司、未知类型、空范围、受限资源及伪造active均拒绝。函数无I/O、无共享可变状态；True不代替实际资源、当前权限、输入许可与额度核验。

命令：`python3 -m unittest discover -s tests -p 'test_workspace_access_policy.py' -v`，加AGENTS中的全套测试、文档/索引/差异检查。最终版本CI与独立审查必须实际核对，命令/环境/时间/退出码/提交留PR；合并后核对main与自动页。

## 未执行与阻塞

本地git远端DNS解析失败退出128，使用连接器读写；本地仅新切片源码/测试和字节核对后的记录副本，不冒称完整clone或旧测试全量本地运行。完整仓库回归由实际CI提供。

登录/成员API、数据库/RLS、实际越权攻击、资源ACL、额度、撤权竞态/恢复、CMS/RAG/云环境和用户宿主加载未运行。负责人为项目负责人；补测条件是进入对应环节、明确接口/安全边界并授权隔离验证。

## 下一步

先按[ACCESS-001](docs/learning/ACCESS-001.md)读Membership→allows_shared_action→只读成员测试。本人状态仍待阅读/待实践；保留的改角色小练习不代做，得到反馈或继续指示后再写下一片。

下一片只讨论当前成员关系的可信来源与调用契约。DATA-001原公告/更正、CMS发布缓存流程保留；不把所有未来参数未定当作暂停本片的理由，也不在本片后自行连写其他模块。
