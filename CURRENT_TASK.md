# 当前任务｜PRODUCT-001

更新：2026-09-12。DOC-004 已由 PR #5 收口；当前是产品范围确认和 RAG 解释，不是业务功能开发。

## 目标

记录负责人确认的中国公开开发/采购/外包机会、软件公司客户和首个核查闭环；讲清 RAG、Agent、业务后端的职责，不把技术建议写成已实现。

## 范围

新增 docs/23-first-market-rag.md 和本任务学习笔记，更新短状态/当前任务并刷新索引。遵循已有持续记录要求，只改文档；不改业务代码、CI或权限，不安装CMS，不采集真实项目、不调用计费模型或部署。本次未请求合并，保留PR供审查，不沿用以前任务的一次性合并授权。

## 验收

中国项目/软件公司/完整首流程明确为已确认；试用者、具体来源、技术和实施参数仍待验证。区分找项目和项目内找证据，解释检索不等于生成、RAG不等于数据库/鉴权/跟进功能；未检索到证据不能证明条件不存在。示例标虚构，保留现有版本、权限、未知项与人工决定边界。

文档索引需重建并通过AGENTS约定的检查；实际完整head、命令、环境、退出码、独立审查及未执行范围留所属PR。无实质代码改动，不为文档任务新增空业务函数或注释。

## 未执行与阻塞

本次 GitHub 的 git ls-remote 因容器DNS失败；远端读写通过连接器，本地仅重建索引所需的已知源码副本，不声称完整克隆成功。最终仓库检查以该PR的实际CI为准。

没有真实采购项目、企业档案、RAG效果、CMS/Redis或业务接口运行证据；来源和真实数据访问条件仍需单独核查。未发现真实试用公司，不虚构用户数量。本人RAG理解/代码实践未确认。

## 下一步

下一产品讨论建议只拆企业档案的最小字段，以及它们如何与一份真实项目的要求逐项对照；这是建议，不是本轮已批准的实现。既定中文CMS“草稿→发布→缓存读取→更新失效→撤下”小流程仍保留，不把CMS做完当成招标研究前置门槛。先完成本PR必要审查与记录；未经本任务放行不合并。
