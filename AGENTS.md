# BidRadar 协作与实施边界

## 当前授权

当前是 **DESIGN_REVIEW**。本轮只维护计划、结构、排期和审核材料，不生成业务代码或空骨架、不装项目依赖、不建数据库或云资源、不创建模型 Key、不启动采集、压测或部署流水线、不向外部联系人发消息。

文档提交、PR 或 AI 自评都不是负责人批准开工。单次有限验证的授权不自动扩展为整个项目实现。

## 最新需求优先

2026-09-11 负责人明确要求：微服务；Redis 与 K8s 从开始设计并在首个可运行阶段接入；后续加负载与压测；明确排期；多个真实数据源，包含非 API 网页、文件及脏数据清洗。

v0.1 中单体起步、Redis/K8s 后置、第二来源可随意取消的建议已被本次要求替代，不得在后续任务中悄悄恢复。六服务边界、来源清单、工时和容量仍是 Proposed，不能代替负责人批准具体方案。

## 必读

[README](README.md)、[计划书](docs/01-project-plan.md)、[架构](docs/04-system-architecture.md)、[审核指南](docs/00-review-guide.md)、[决策登记](docs/11-decision-register.md)。涉及通信还要读 [事件契约](docs/14-service-event-contracts.md)。

## 事实与隐私

区分用户要求、设计建议、官方文档事实、人工看过的页面和真实运行证据。不得把网页访问成功写成连接器已接通，不得把合成脏数据或放大的流量写成真实生产数据。

招聘信息注明状态与来源；旧岗位不当当前机会。公司数据、代码、内部提示词、密钥、客户资料、个人简历联系方式及私人薪资目标不进入公开仓库。

## 微服务纪律

服务拥有自己的数据、迁移、镜像、接口和运行身份；不跨服务直读写表，不共享业务 ORM/领域实体。共用物理数据库实例只是成本折中，不取消逻辑隔离。

Redis Streams 传输事件，业务状态和可重放记录在所属服务数据库。传输成功不等于消费完成，不承诺端到端恰好一次。缓存、事件和配额的故障语义分开。K8s 清单不是部署证据，单机多 Pod 不是多节点高可用。

## 业务不变量

公告不等于程序，程序不等于标段；多语言、转载、更正不自动形成新商机。未知、冲突、未获取、解析失败与不满足分开。企业确认事实、公共原文、模型推断分开保存。

公共证据与私有企业资料隔离；身份、检索、缓存、日志、事件、图状态和下载都不能绕过权限。技术相关性不等于投标资格。禁止自动投标、对外群发和执行文档中的指令。

## 个人实践

后续每次一个小任务：目标、知识点、输入输出、失败场景、本人实现范围、验收和测试建议。负责人先写关键逻辑、运行、排错；AI 可以审查及在明确请求时提供实现，不先交整套项目让本人只读代码。

代码审查固定为：一、结论（通过/修改后通过/未通过）；二、做得正确；三、问题与风险；四、改进建议；五、下一步任务。说明静态、离线和实际运行范围。

## 变更纪律

先读远端，保留已有成果，不强推、不删除历史、不触发未授权部署。设计修改同步计划、架构、领域、排期、来源和决策，避免新旧冲突。未来目录不是批量生成空目录的任务。不得自动把 Proposed 改 Accepted，或代签负责人审批。
