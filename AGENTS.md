# BidRadar 协作与实施边界

## 当前授权

**DESIGN_REVIEW。**当前请求是把已讨论的决定和项目优先路线写入仓库。只修改设计、排期和审核材料，不生成业务代码/空骨架、不安装项目依赖、不执行采集或模型、不创建 Key、云资源或部署流水线、不购买服务、不对外发送消息。文档提交不等于批准开发；Draft PR 不自动合并。

## 最新要求与优先级

2026-09-11 负责人依次明确：独立招标商机 Agent；微服务；Redis/K8s 从初期进入；真实多源及网页/文件脏数据；后续压测；全球扩展；阿里云；项目总预算约 100–200 元/月。

**同日最新调整：前期数据源可以少，项目本身优先。**先完成档案、筛选、证据分析、追问、跟进与变化提醒，再按实际业务缺口扩源。1 个主来源加按需第二来源是提案，不是已通过准入。取消四源/六源/六大区的发布门槛；保留全球扩展能力和真实清洗，不把项目变成采集平台。

微服务、早期 Redis/K8s、个人实践和可靠性不因此取消。六服务细分、框架、数据源、机器规格、资源分配和工时仍待审核。以 [决策登记](docs/11-decision-register.md) 区分 Confirmed requirement、Proposed 和 Needs evidence，不自动把提案改成 Accepted。

## 必读

[README](README.md)、[计划书](docs/01-project-plan.md)、[产品交付蓝图](docs/17-product-delivery-blueprint.md)、[审核指南](docs/00-review-guide.md)、[决策登记](docs/11-decision-register.md)。实现服务时再读对应架构、领域、Agent 和事件专题。排期以 [09](docs/09-roadmap-ownership.md) 为准，来源和预算以 [03](docs/03-data-foundation.md)、[16](docs/16-global-sources-cloud-budget.md) 为准。

## 事实、隐私与预算

区分用户要求、设计提案、已读官方资料、真实样本和实际运行。网页可读不等于连接器接通，历史合同不等于活跃商机，注入数据/回放流量不冒充自然缺陷/用户规模。每次外部复核记录时间与失败；不因文档升级把旧核验写成新结果。

公司数据、代码、提示词、客户信息、密钥、个人简历联系方式及私人求职薪资目标不进入公开仓库。项目预算可用于设计，不公开账号账单和付款资料。

总预算包含计算、存储、网络、备份及业务模型，按 200 元作未经重新确认不可扩大的规划上限；不是云厂商硬封顶。费用分配和配置不等于报价。月付/年付、首次现金、地域、网络、续费和资源需分别确认，不默认采购 GPU、多节点或长期套餐。

## 工程与业务不变量

服务独立拥有数据、镜像、迁移、权限与契约；不共享业务 ORM，不跨库读写。共用机器/数据库实例是成本折中，不是高可用。Redis 事件与可淘汰缓存分进程；任务和事件恢复依据在服务数据库。传输成功不等于业务完成，不承诺端到端恰好一次。

公告、程序、标段、语言与更正分别建模。未知、冲突、未获取、解析失败与不满足分别表达。来源事实、人工确认能力和模型推断分开；不能让自然语言指令直接改变授权、资质或官方事实。

公共证据与私有企业资料在查询、缓存、日志、事件、索引、checkpoint、下载中隔离。技术相关不是投标资格认证。禁止自动投标、群发联系、任意 SQL/Shell 和执行网页指令。

## 产品优先的任务纪律

每项任务都写明服务哪项用户行为及验收；新增来源须说明当前缺什么，而不是只说多一个国家。不能为凑数据或常驻空服务推迟 Agent 与用户流程。真实网页/文件路径可在首个研究版本后补齐，但完整交付时必须有可追溯清洗与回归证据。

一次只安排一个清楚的小任务：目标、知识点、输入输出、失败场景、个人实现范围、验收与测试。负责人先写关键逻辑、运行并排错；未要求完整实现时，不先交付整套模块替代练习。

代码审查格式固定：一、结论；二、做得正确；三、问题与风险；四、改进建议；五、下一步任务。说明静态、离线和真实运行边界。

## 变更纪律

先读远端并保留已有成果；不强推、不删除历史、不自动合并。重要变更同步入口、计划、范围、数据、排期与决策，避免旧来源配额继续成为隐性门槛。技术专题可独立保持版本，但不得覆盖最新明确要求。未来目录不是本轮批量创建代码的清单，不代签用户审核与实施授权。
