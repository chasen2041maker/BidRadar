# BidRadar 协作与实施边界

## 当前授权范围

当前阶段为 **DESIGN_REVIEW**。项目负责人明确要求先写计划、认真审核结构和基础，不急于开发。

在负责人另行授权前，只维护设计与审核文档：不生成业务代码或空代码骨架，不安装项目依赖，不创建数据库、云资源、模型 API Key、部署流水线，不启动采集任务，不自动向外部发送信息。一次文档提交、AI 自评或复述需求，都不是负责人批准开工的记录。

后续负责人可以明确授权单个数据/技术验证；该授权不自动扩展到完整实现。实际任务的最新明确指示优先，范围变化须同步记录。

## 必读与事实边界

先读 [README](README.md)、[计划书](docs/01-project-plan.md)、[审核指南](docs/00-review-guide.md) 和 [决策登记](docs/11-decision-register.md)。

始终区分：用户已确认要求、设计提案、官方来源事实、实际运行证据。不得把 Proposed 改为 Accepted，也不得代填用户批准、评测成绩、试用人数或部署成果。

外部数据与招聘资料需注明来源和核验时间。无法读取或验证时，保留未确认状态；不得用过去聊天中的肯定语气代替证据。公司内部数据、代码、提示词、客户资料以及个人简历联系方式不进入仓库。

## 个人学习与代码责任

负责人亲自写核心逻辑，亲自接数据、运行、调试、验证并修正。AI 负责方案讨论、解释、审查、测试设计与文档整理，可在明确请求时辅助代码。

后续一次只提出一个边界清楚的实现任务，包含目标、知识点、输入输出、失败场景、验收和测试建议。除非明确要求完整实现，不直接交付整个可运行模块代替本人练习。不能用生成代码行数衡量掌握程度。

代码审查固定结构：一、结论（通过/修改后通过/未通过）；二、做得正确；三、问题与风险；四、改进建议；五、下一步任务。只报告有证据的问题，区分静态阅读与实际运行。

## 设计不变量

- 公告不等于采购程序，程序不等于标段，语言版本不等于新商机。
- 不把未知、冲突、解析失败当成满足或不满足；保留证据及版本。
- 技术匹配不是投标资格认证；不输出无依据中标概率。
- 原始事实、用户确认的能力、模型推断分别保存。
- 公共公告与私有企业资料隔离；向量、缓存、日志、图状态都不能绕过权限。
- 已接受任务持久化；重复执行可能发生，但不得重复发布内部结果或提醒。
- 不从提示词获得写权限，不自动投标或联系采购方，不运行任意网页提供的指令。

## 变更纪律

先读取远端实际内容，保留已有成果；不强推、不删除历史、不触发未授权部署。架构重要变化先写决策理由、替代方案、影响和验证条件。

文档中的未来目录不是待批量创建的清单。先通过设计关口，再按里程碑逐步增加文件。任何来源链接、数字阈值和功能状态变化都要保持跨文档一致。
