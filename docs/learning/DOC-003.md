# DOC-003｜证据化研发规范与短上下文接棒

摘要：已合并研发规范与交接骨架；核心经验是最终提交独立审查、真实验证与个人学习进度必须分开。

## 本次改变

历史任务：PR #3 合并于 2026-09-12，main 提交 58069ae4df577bef32dcca7090d3b4df9bfe8b38，含 PR #2 的后台/CMS/缓存设计。只包含规范和治理工具，不是业务系统已经运行。

## 阅读顺序

先看 `AGENTS.md` 的完成标准，再看 `scripts/check_project_memory.py::check` 如何检查文件结构，最后看 `tests/test_project_memory.py` 中围栏边界的负例。本轮不要求通读 20 多份设计材料。

## 数据流与失败边界

设计约定 → 工具检查 → 独立审查发现漏检 → 回归修复 → 最终 head 复审 → 合并。开发自检不是独立审查；文件存在不是宿主已加载；治理脚本成功不是 CMS/Redis 已通过。

## 验证与未验证

最终已审 head 为 5e3e6b8505f4343685d34f60c240066538fdd478。GitHub Actions 的 30 项治理工具测试及合并后检查通过。[实际记录](https://github.com/chasen2041maker/BidRadar/pull/3#issuecomment-5645099106)。这是历史验证，不能自动沿用到后续提交。中文 CMS、缓存、业务接口、部署均未运行验证。

## 我的参与

待阅读/待实践；没有本人已手改或运行这些代码的确认。不把已合并的规范当作本人已经掌握。

## 值得保留的决定与坑

不把所有历史塞进 AGENTS；规则、短状态、单任务与按需知识分工。独立审查发现字节码污染、围栏尾部/缩进及普通文本链接漏检；修复后必须重跑最终提交而不是沿用旧测试成绩。
