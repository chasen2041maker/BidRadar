# SOURCE-001｜两阶段流程、来源台账与CCGP离线复用

摘要：确认目录筛选后等待用户选择再深度分析；更新01/17/25及来源台账，改编一个MIT许可的CCGP离线参数/列表解析模块，22项虚构样本测试通过，不冒充采集或持久任务已接通。

## 本次改变

用户要求把两阶段流程和此前竞品借鉴写入计划书及相关docs，并直接复用合适的数据源部分。01/17同步产品闭环与验收，28明确选择状态、来源核查和复用决定；25保留旧样本并增补当前边界。来源候选JSON是文档台账，全部network_enabled=false。

只新增1个核心实现文件和1个测试文件。复用bidding-ai-analyzer的CCGP参数键、列表选择器和元数据拆分思路，保留固定上游提交、版权和完整MIT；改成标准库离线片段，不搬全局任务字典、线程、默认全分析或并发模型。其他竞品代码/数据未导入。

## 阅读顺序

1. [ccgp.py](../../services/ingestion/sources/ccgp.py)::build_search_params：关键词、页码和发布日期区间如何变为查询参数，为什么只生成参数、不执行网络请求。
2. 同文件parse_search_page：输入HTML→确认列表模板→逐条校验→保留文本/原链接/位置/指纹→区分ok/empty/partial/blocked/parse_error。
3. [test_ccgp_source.py](../../tests/test_ccgp_source.py)::test_block_page_is_not_empty与test_dom_drift_is_not_empty：看“无法读到列表”为什么不能报告“零商机”。

设计阅读入口是[01](../01-project-plan.md)第5节与[28](../28-two-stage-and-source-reuse.md)第2节；本轮不要求通读其他历史。

## 数据流与失败边界

本片只有关键词→参数字典，以及手写HTML→目录线索，无I/O/模型/数据库。未来获准采集须先归档原件并记录实际获取时间，再把HTML传入；URL白名单不能替代DNS/重定向安全。元数据原文不是完整证据链，发布日期不是响应截止，模型/资格判断不属于本片。

目标流程为公共目录→公司私有选择会话awaiting_decision→明确稳定ID/版本选择→当前授权/输入许可/额度→research持久任务。空选择不分析全部，重复命令不重复建任务，列表刷新不调用模型；这些业务链尚未实现。自动跟踪复核需要独立有效委托，不能借后台任务绕过选择授权。

## 验证与未验证

本地局部目录`/mnt/data/bidradar-source-task`；Python3.13.5；2026-09-15T06:02:11Z执行`python3 -m unittest discover -s tests -p 'test_ccgp_source.py' -v`，退出0，22 tests / OK。全部HTML是手写虚构样本，日志位于工作容器`/mnt/data/bidradar-source-tests.log`，PR保留精确版本和结果摘要。

受测ccgp.py Git blob：77ecdd3bb288c278276027807757500e42274ee3；测试blob：0d9e18031d13845c7d3b68ae336fea634bafeb38。最终提交后须按这些字节核对，不把局部目录冒称完整clone或本地真实HEAD。

git/raw/codeload访问尝试因DNS失败；连接器负责真实仓库读写。原AGENTS、五份旧笔记和refresh_project_memory.py按已有Git blob逐字节核对，作为本地生成/检查索引的输入。完整仓库治理测试、最终提交--base及diff检查以实际PR CI为准，不沿用PR #8的64项结果，也不提前填独立审查通过。

未运行：真实CCGP列表/分页/正文/附件、来源准入/自动采集、原件归档、选择页面、数据库持久任务、HTTP鉴权/RLS/撤权竞态、模型/费用、CMS/Redis/K8s和云环境。负责人为项目负责人，补测条件是对应小片授权、访问用途和隔离环境。网页尝试失败、海外官方文档及许可证核查详见28；文档可读不证明客户端联调。

首候选74634bf7b7682f7329f166dd97b71f810b30fc8d的push CI（job104273418534）在结构检查退出1：PROJECT_STATE/CURRENT_TASK标题未沿用固定约定，后续测试被跳过。本次恢复待验证/证据入口及目标/范围/验收/未执行与阻塞/下一步标题，不降低治理规则；修订后须重跑最终head完整CI。

## 我的参与

负责人已确认两阶段流程，并授权本次文档及可复用来源代码更新。本人仍待阅读/待实践，不把AI编写、单测或PR状态标成本人已掌握。ACCESS-001改角色练习未代做。

留一个阅读问题：若网站返回验证码页，为什么必须区别于“本次查询确实共0条”？可在自己的隔离练习分支把一个正常测试HTML换成验证码文本，先预测状态再运行；本轮不替负责人做该练习。

## 值得保留的决定与坑

复用的是适合当前需求且许可明确的小块，不是上游完整架构。源码里有域名、配置enabled=true或README声称全国监控，都不能证明网站可采集。软件许可、数据权利、真实响应、稳定分页和商业效果分别记录。

发现的反例包括空selected_indices可能触发全部分析、内存字典不提供持久恢复、快照缺原URL、配置中央公告却指向地方目录、README许可徽章与LICENSE冲突、SAM上游路径/参数与官方文档不同。对应取舍写在28与第三方登记，不复制这些问题。

本轮通过分支/PR交付，独立审查和人工合并放行仍需实际证据；没有常态自动合并、批量采集或部署授权。后续只选一个小片，先补来源准入/真实样本或选择到任务契约，不连续铺多个模块。
